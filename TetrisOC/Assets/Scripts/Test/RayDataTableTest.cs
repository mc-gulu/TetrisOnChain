using MMGame;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RayDataTableTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log(CalculateTool.CalcMathFunc("LOG(LOG(2,4),2)+POW(2,3)+1"));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
